# Core dependencies
mcp>=1.0.0
litellm>=0.1.0
pydantic>=2.0.0
python-dotenv>=0.21.0

# HTTP client with HTTP/2 support
httpx[http2]>=0.25.0

# Additional dependencies that might be needed
asyncio
typing-extensions 